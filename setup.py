from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='AI-powered DJ that generates mixtapes from a playlist of songs',
    author='Dennis Dang',
    license='MIT',
)
